# backend/app/backtest/cbo/__init__.py
#
# CBO_V1 — previous-candle breakout on index spot.
# Pure engine: cbo_v1_engine (no app imports, no DB, no I/O).
# Runner: backtest_cbo_runner (added in the next stage).
