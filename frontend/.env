# Prevents react-scripts (CRA) from auto-opening a browser tab on `npm start`.
# The UI is served inside the Tauri webview window — a browser tab is redundant.
BROWSER=none
DANGEROUSLY_DISABLE_HOST_CHECK=true
